{
  "name": "hoverlab",
  "version": "0.3.0",
  "description": "Install UI from the Hoverlab catalog — CSS effects, React blocks, whole pages, and scaffoldable project templates — and expose them to your editor's AI agent over MCP.",
  "keywords": [
    "css",
    "animation",
    "hover",
    "ui",
    "components",
    "blocks",
    "templates",
    "scaffold",
    "boilerplate",
    "tailwind",
    "react",
    "nextjs",
    "vue",
    "svelte",
    "mcp",
    "model-context-protocol"
  ],
  "homepage": "https://hoverlab5.netlify.app",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Vijetbhat6/hoverlab.git",
    "directory": "packages/cli"
  },
  "bugs": {
    "url": "https://github.com/Vijetbhat6/hoverlab/issues"
  },
  "license": "MIT",
  "type": "module",
  "bin": {
    "hoverlab": "./bin/hoverlab.mjs"
  },
  "exports": {
    ".": "./src/index.mjs"
  },
  "files": [
    "bin",
    "src",
    "README.md"
  ],
  "engines": {
    "node": ">=18.17"
  },
  "//": "Intentionally dependency-free. `npx hoverlab add btn-gradient` should be one round-trip to npm and one to the API — every dependency added here is latency a user feels before they get their effect. This is also why `init` reads a template as JSON rather than unzipping the archive the website serves.",
  "dependencies": {},
  "scripts": {
    "test": "node --test",
    "//prepublishOnly": "Runs on `npm publish` and not on `npm install`, which is the whole point of choosing this hook. This package is the install path for the CLI, the MCP server and the editor extension, and npm publishes are effectively permanent — unpublishing is restricted to 72 hours and leaves the version burned. 128 tests take about a second; a broken `add` reaching npm costs a version number.",
    "prepublishOnly": "node --test"
  }
}
